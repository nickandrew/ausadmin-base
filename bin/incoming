#!/bin/bash
#	@(#) incoming - Save a received message and process it

cd ~ausadmin

ts=`date '+%Y%m%d-%H%M%S'`
suffix=`printf "%05d" $$`
fn=messages/$ts.$suffix

cat > $fn

bin/incoming.pl $fn | bin/collater.pl
