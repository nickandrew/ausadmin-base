#!/bin/bash
#	@(#) incoming - Save a received message and process it
#	@(#) $Header$

cd ~ausadmin

export PATH=$HOME/bin:$PATH

ts=`date '+%Y%m%d-%H%M%S'`
suffix=`printf "%05d" $$`
fn=messages/$ts.$suffix

cat > $fn

incoming.pl $fn | collater.pl
